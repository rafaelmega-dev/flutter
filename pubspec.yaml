name: helloworld
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  geolocator: ^14.0.3
  go_router: ^14.6.0
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Caso crie as pastas "assets/images", "assets/icons" ou "assets/data" 
  # na raiz do projeto, descomente as linhas abaixo:
  # assets:
  #   - assets/images/
  #   - assets/icons/
  #   - assets/data/